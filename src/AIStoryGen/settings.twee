:: AIStoryGen_Config
<h2>织境空间 设置</h2>

<p class="ai-cfg-hint">
  <b>提示：</b>你也可以在游戏主 <span class="gold">设置</span> 页或侧边栏
  <span class="gold">OPTIONS</span> 浮层中找到
  <span class="gold">AI 设置</span> 标签页。<br>
  所有设置保存在浏览器的 localStorage 中（不存入游戏存档）。
</p>

<<aiconfig>>

<p class="ai-cfg-hint">
  在任意 passage 中使用：<br>
  <code>&lt;&lt;aigen "描写玩家当前看到的场景"&gt;&gt;</code>
</p>

[[返回标题|Start]]


:: AIStoryGen_Demo
<h2>织境空间 演示</h2>

<p>请先确保已在 <span class="gold">设置 → AI 设置</span> 中配置了 API 密钥
（或点这里：[[AIStoryGen_Config]]）。</p>

<h3>直接生成文本</h3>
<p>在任意 passage 中写入 <code>&lt;&lt;aigen "指令"&gt;&gt;</code> 即可生成文本。</p>

<div class="ai-gen-box">
  <<aigen "用简短的大气描写描述玩家当前所处的环境。">>
</div>

<h3>分支选项</h3>
<p>在任意 passage 中写入 <code>&lt;&lt;aichoices "可选侧重点"&gt;&gt;</code> 即可生成 5 个分支选项。</p>

<<aichoices "侧重社交互动和探索">>

<p>
  [[打开织境空间设置|AIStoryGen_Config]] | [[返回标题|Start]]
</p>
