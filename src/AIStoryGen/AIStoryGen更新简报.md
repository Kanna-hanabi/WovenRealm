## 0.1.318 / AIPixelGen 0.1.111
- AI 分支选项增加原版风格耗时编辑：每个选项后显示 `(小时:分钟)`，小时和分钟可以单独修改。
- 点击选项时使用玩家最终填写的耗时，并通过原版 `<<pass N>>` 推进时间；发送给 AI 的选择文本也会同步为修改后的 `(H:MM)`。
- 自定义剧情输入行也增加同样的耗时编辑，默认按动作关键词预估，玩家改动后优先使用手动值。

## 0.1.316 / AIPixelGen 0.1.111
- 统一 NPC 中文名规范：剧情、选项和后处理会把 `Sydney` / `西德尼` 统一为游戏内中文名“悉尼”。
- 主剧情脚本复用角色检测模块导出的标准 NPC 中文名表，避免不同模块各自维护名字导致同一角色出现多个译名。
- AI 道具识别补充“悉尼/西德尼”人名过滤，减少人物名被误判为可拾取道具的情况。

## 0.1.315 / AIPixelGen 0.1.111
- 修复旧版 DoL 兼容报错：老版本或旧存档中眼色/自定义眼色缺少 `canvasfilter` 时，会在兼容层补齐安全默认滤镜。
- 旧版中如果 `$leftEyeColour` / `$rightEyeColour` 指向不存在的颜色名，会回退到原版可用眼色，避免角色外观渲染时报 `Cannot read properties of undefined (reading 'canvasfilter')`。
- 该修复只补缺失或无效字段，不改写已有合法颜色和游戏流程。

## 0.1.314 / AIPixelGen 0.1.111
- 金钱变化增加“双确认”校验：AI_EVENT moneyChange 必须是 signed 数字，并且正文中必须明确写出同方向、同金额和金钱原因，否则字段会被丢弃。
- AI_EVENT 的 statChanges 不再接收 money，旧式 [STATS: money+...] 不能独立触发金钱变化；只有通过校验的 moneyChange 会真正应用。
- 提示词进一步要求不要猜测挑战奖励/花费，进入挑战、搜索、发现等动作不会自动产生金钱变化。
## 0.1.313 / AIPixelGen 0.1.111
- 金钱变化改为显式规则：不再根据“找到/发现/获得”等关键词兜底推测金钱增加，也不再默认补 +50。
- 提示词要求 AI 若产生金钱变化，正文必须写清楚具体金额和原因，并在 AI_EVENT 的 moneyChange 写 signed pence；金额不明确时必须留空。
- 给 AI 的状态白名单拆出非金钱列表，避免模型继续把 money 当作普通 [STATS] 字段输出。
## 0.1.312 / AIPixelGen 0.1.111
- 修复 AI 金钱变化误触发：旧式 [STATS: money+...] 只有在 AI_EVENT 明确确认、正文明确写出金额，或正文明确获得/支付金钱时才会应用，避免侧边栏金额或模型误标记导致凭空加钱。
- 修复金钱变化显示颜色：金钱增加显示绿色，金钱减少显示红色；压力、疼痛等普通负面属性仍保留“增加红、减少绿”的通用提示色。
## 0.1.311 / AIPixelGen 0.1.111
- 收紧生图正向清理：保留 book/paper 作为可画道具，不再把旧书、信纸、纸张统一替换成 plain surface；只要求画面中的纸张/墙面/标牌不出现可读文字。
- 收紧道具提取：不再因为普通“灰烬气味/空气”就加入炉火、余烬、苔藓等道具，避免非炉边场景混入炉火元素。
## 0.1.310 / AIPixelGen 0.1.110
- 重构剧情场景图 prompt 拼装：新增 SceneVisualFacts 事实包，统一管理剧情事实、游戏地点状态、主角外观、可见角色、动作、道具、光照和镜头。
- 最终生图 prompt 和 LLM 结构化 prompt 都改为消费同一份事实包，减少旧 prompt、隐藏兜底和风格规则互相覆盖导致的地点/动作漂移。
- 关键动作和道具提取改为以当前剧情为主，玩家风格和绘图风格只追加风格约束，不再改写当前剧情事实。
## 0.1.309 / AIPixelGen 0.1.109
- 修复剧情场景图误判实验室：不再把普通“空气/气味”描写当作实验室线索，只有化学、实验室、试剂器材等明确组合才进入实验室画面。
- 增加庄园书房、走廊、书架、纸墨、旧书、焦痕类动作识别，当前剧情为书房探索时不再沿用实验室 action。
## 0.1.308 / AIPixelGen 0.1.108
- 生图正面提示词默认加入“near-modern British setting, contemporary English interiors and architecture, grounded small-town British environment style”，用于统一 DoL 场景的近现代英伦环境风格。
- 读取旧绘图配置时会自动补入这条风格提示，避免旧浏览器配置继续缺少环境时代感约束。
## 0.1.307 / AIPixelGen 0.1.107
- 修复 AI 选项解析混乱：支持模型返回二维数组或对象格式，例如 [["(0:05)", "你走进走廊"]] 会正规化为“你走进走廊 (0:05)”。
- 避免时间被放到选项开头、JSON 片段直接显示在按钮上，减少 “(1)(0:05)\",\"文本” 这类错位选项。
## 0.1.306 / AIPixelGen 0.1.106
- 放宽生图“调整提示词”的中文/英文编辑态长度：翻译输入和输出上限从 3600 字符提升到 12000 字符，避免中文编辑框只显示半段提示词。
- 生图提示词中英互译的 LLM 输出下限从 1200 tokens 提升到 4096 tokens，减少翻译中途截断。
- 最终发给生图模型的 prompt 仍保留模型兼容压缩，避免图像 API 因超长提示词失败或忽略后半段。
## 0.1.305 / AIPixelGen 0.1.105
- 修复生图提示词文本框残留旧场景的问题：剧情场景图按钮每次点击都会重新读取当前页面的剧情源和缓存 key，不再沿用控件创建时的旧闭包数据。
- 最终生图 prompt 增加场景冲突校验：当前页面是卧室时会拒绝实验室/浴室 action，当前页面是浴室或实验室时也会拒绝明显不匹配的旧 action。
- 生成提示词后会再次走结构化校验，避免 LLM 或旧文本框把上一场景动作带入当前图。
## 0.1.304 / AIPixelGen 0.1.104
- 修复剧情场景切换后生图面板继续显示上一页旧图的问题：生图入口现在会按当前 passage、模型、端点和剧情提示词生成 key，发现旧 key 时会清掉旧面板并重建。
- 生图结果节点补写当前 key，方便后续调试和防止旧结果误判为当前页面结果。
- 补充凯拉尔庄园、约旦神殿房间、狼洞等级等子地点中文显示名，减少“选择实际到达的原版地点”里漏出英文 passage 名。
## 0.1.276 / AIPixelGen 0.1.76
- 生图菜单默认风格改为 DoL 像素风；剧情场景图和姿态图也默认使用 DoL 类型，不再默认切到视觉小说风格。
- 玩家在生图菜单中选择过的风格会保存到绘图配置，后续同类生图自动沿用，避免每次重新选择。
- 生图提示词菜单新增“调整提示词”：先把英文提示词翻译成中文供玩家修改，点击“确定并生成图片”后再翻译回英文并开始生图，避免中文 prompt 直接进入生图模型。
## 0.1.275
- 第一版测试包默认隐藏色色/战斗辅助相关内容：设置页不显示 AI 色色模式、色色模块模式、色色宽容度、AI 生成色色动作、战斗 AI 叙事和战斗叙事提示词。
- 游戏内默认不显示“进入色色”和“自定义色色”两个入口，也不会注入 AI 战斗叙事、额外动作和结束战斗辅助控件。
- 在“剧情AI设置”页底部连续点击“织境空间 v...”版本号 20 次后，隐藏内容会在本地浏览器解锁；内部包名和存档兼容保持不变。
## 0.1.274
- 发布测试包命名调整：玩家可见名称改为“织境空间”，英文昵称为 “Woven Realm”。
- 保留内部包名 `AIStoryGen`、章节 ID、存档字段和缓存键不变，避免破坏旧配置、旧存档和 ModLoader 缓存兼容。
- 设置页标题、重置确认、版本显示和 readme 同步改为“织境空间”。
## 0.1.273 / AIPixelGen 0.1.75
- 发布前清理：修复 `boot.json` 中文昵称乱码，测试包在 ModLoader 列表中显示为“AI叙事生成”。
- 发布前清理：打包脚本不再把未被 `boot.json` 加载的开发别名 `aiMacro.js / ui.css` 放入 zip，降低测试包重复文件和误判风险。
- 生图模块不再默认向控制台输出最终生图 prompt，避免测试版玩家控制台刷屏或泄露个人设定。
- 同步更新 readme 和包版本，便于第一版测试发布核对。

## 0.1.272 / AIPixelGen 0.1.74
- 将当前浏览器中的非敏感 AIStoryGen/AIPixelGen 设置写入默认配置；排除 API 密钥、破甲词、本地 ComfyUI Endpoint 和本地 Checkpoint 文件名。
- “AI 生成色色动作选项 (?)”移动到“色色宽容度 (?)”下方。
- `AIStoryGen v...` 版本号移动到“剧情AI设置”页最底部，提示词页和绘图页不再显示该版本号。
- “恢复默认”按钮改为真正使用新的默认配置，不再额外强制把色色宽容度改成宽松。

## 0.1.271
- 将绘图设置中的“瑟瑟场景图”文案统一改为“色色场景图”。
- 调整绘图设置顺序：先显示“剧情场景图生成方式”，再显示“色色场景图启用模式”。

## 0.1.270
- 修复 `AIStoryGen_Intimate_Mode` 被原版软锁检测误判为“没有可用链接”的问题：该自定义 JS 驱动页面现在使用原版 `exitCheckBypass` 标签跳过普通剧情链接检查。
- 重写 `ai-intimate.twee` 为干净 UTF-8 文本，并修复兜底“返回原版章节”链接乱码。

## 0.1.269 / AIPixelGen 0.1.73
- 将绘图设置里的“特殊场景生图设定”改名为“瑟瑟场景图生图设定”。
- 同步调整该区域的启用模式、关闭/开启选项和姿势控制文案，避免玩家误以为这是普通剧情场景图设置。

## 0.1.268
- 自定义 AI 色色叙事改为更接近原版色色页节奏：提示词要求 3-5 个自然段，通常约 450-800 个中文字，补足动作过程、停顿、反应和状态变化。
- 自定义 AI 色色生成调用的 `max_tokens` 下限从 1100 提高到 1600，减少剧情刚展开就被截短的问题。
- “补充画面”面板改为稳定显示在自定义色色页里；即使没有检测到明确视觉缺口，也会作为“可选生成当前回合补充画面”显示，避免玩家找不到入口。

## 0.1.267 / AIPixelGen 0.1.72
- 自定义 AI 色色页新增“补充画面”兜底：当剧情含有原版动态图难以完整覆盖的姿势、环境、道具或机器细节时，会在原版动态图下方显示 AIPixelGen 生图面板。
- AIPixelGen 新增 `renderInlinePixelPanel` 公共接口，AIStoryGen 可以在指定位置嵌入生图按钮，同时继续复用玩家绘图配置、缓存、风格和模型。
- 补充画面不会替代原版动态图，只用于表现原版没有对应图层的细节；原版能同步的身体接触、机器、道具图层仍优先走原版资源。

## 0.1.266
- 新增原版视觉能力表 `ai-visual-capabilities.generated.js`，由 `tool/build-visual-capabilities.js` 从新版游戏 `img/sex` 资源扫描生成，当前覆盖 `close / machine / prop / xray` 共 1010 个图片资源。
- 自定义色色提示词新增 `availableVisualCapabilities` 摘要，要求 AI 只把原版确实存在的图层写入 `visualContact / visualMachine`；不存在的画面保留正文，后续可交给生图或文字兜底。
- 机器/道具补充层渲染前会查询视觉能力表，避免游戏版本缺少某张资源时出现破图或无效动态图。

## 0.1.265
- 修复旧 AI 色色场景对象已有“空机器状态”时，不会从存档镜像补回吸奶器/奶罐等视觉状态的问题。
- 增加机器视觉状态非空判断，避免回退、读档或测试注入时被默认空对象挡住补图逻辑。

## 0.1.264
- 自定义色色动态图预览新增机器/道具补充层：AI 剧情提到吸奶器、挤奶器、乳汁、奶罐、阴茎挤奶器、阴道/后穴机器时，会优先显示原版 `machine` / `prop` 资源。
- AI 自定义色色提示词新增 `visualMachine` 字段，鼓励模型结构化回报“吸奶器=乳汁、奶罐=4、阴道机器=插入”等状态；模型漏填时也会从正文和玩家动作里推断。
- 机器补充层只用于视觉预览，不写入原版战斗变量，避免 AI 自定义模式污染真实原版战斗流程或存档。

## 0.1.263
- 自定义色色动态图预览新增“接触状态层”：AI 剧情提到入口、插入、口部、后穴、胸口摩擦等状态时，会临时同步到原版动态图变量，让剖面图/接触图能跟随叙事出现。
- AI 自定义色色提示词新增 `visualContact` 字段，鼓励模型结构化回报“阴道=插入、后穴=入口、嘴部=插入”等状态；模型漏填时也会从正文和玩家动作里推断。
- 预览渲染后会恢复 `vaginastate/anusstate/mouthstate/penisstate` 等原版变量，避免 AI 自定义模式污染真实原版战斗或存档。

## 0.1.262
- 自定义色色动态图预览新增“视觉状态层”：AI 剧情里出现脱上衣、脱下装、只剩内衣、全裸等描述时，会临时同步到原版动态图预览；渲染结束后立刻恢复 `V.worn`，不会真正脱掉玩家装备或污染存档。
- 修复姿势同步优先级：新一轮 AI 输出的 `pose / sceneState / 正文 / 玩家动作` 会优先参与 `visualPose` 推断，避免旧姿势压住后背位、靠墙等新剧情。
- AI 自定义色色提示词新增 `visualClothing` 字段，让模型主动回报“按原版穿着/只剩内衣/上衣已脱/下装已脱/全裸”等视觉状态，减少图片与剧情脱节。

## 0.1.261
- 自定义色色页去掉“原版动态图预览”标题，并压缩动态图容器的边距/边框，减少画面上方看起来空出的格子。
- 自定义色色动作栏会读取主角 `player.penisExist` / `player.vaginaExist` 等原版变量：同时拥有阴茎和阴道时分别显示“阴茎”“阴道”两个栏位；只有一个器官时只显示对应栏位；无法判断时保留通用“性器”兜底。
- AI 生成下一轮动作的结构化提示同步改为按当前主角器官动态列出栏位，避免 AI 仍只输出一个笼统“性器”组。

## 0.1.260
- 修复自定义色色页没有原版动态图的问题：原版命名 NPC 数据实际在 `V.NPCName`，不一定在当前 `V.NPCList`，动态图捕获现在会先匹配命名 NPC，再使用当前战斗槽位。
- 旧场景/旧存档进入自定义色色页时，如果缺少动态图快照，会在渲染时按当前对象自动补拍一次，不需要重新发起场景。
- 继续保持“能接原版角色就接，接不到就空着”的策略，不伪造 NPC，避免污染原版状态。

## 0.1.251 / AIPixelGen 0.1.71
- 检查设置页所有可填写提示词位置，确认剧情 AI 与生图 AI 的主要提示词字段都能进入对应请求或处理链路。
- 修复生图“风格关键词”在 AI 剧情旁生图路径中未参与最终风格提示的问题；现在玩家把该字段改成非默认值时，会追加到故事生图的风格 prompt，并纳入缓存签名。
- 保持默认旧像素风关键词不强行污染故事场景图，避免与视觉小说/场景插图风格冲突。

## 0.1.251
- 调整“文风设定”交互：下拉框新增“使用自定义文风”，选预设时下方直接显示对应提示词，选自定义时才允许编辑。
- 文风设置继续采用手动保存，避免玩家浏览预设时自动改动当前配置。
- 保存预设文风时不再把预设文本写入自定义框，减少后续切换文风时的混淆。

## 0.1.250
- AI 设置的“提示词”页新增“文风预设”和“自定义文风设定”，可为剧情生成指定极简、史诗、cult 或玩家自写文风。
- 文风设定只接入 AI 剧情正文、地点过渡、剧情刷新和独立 AI 色色叙事，不影响连接测试、长期记忆精炼等后台任务。
- 文风提示增加事实优先约束，避免风格要求覆盖地点、NPC、物品、数值变化和 AI_META / AI_EVENT 等系统格式。

## 0.1.249
- 修复场景生图 prompt 内部地点冲突：当前剧情出现雪林/小屋时不再被当前原版 passage 强行覆盖成神殿；普通 Temple 与 Temple Jordan 分开识别。
- 场景图玩家外观不再默认把内衣作为可见服装重点，除非没有外层衣物，减少生图露出内衣导致画面偏题。
- AI 扩写生图 prompt 时不再覆盖系统判断出的地点、玩家外观和核心动作，只作为道具/光线/镜头辅助；同步更新缓存签名避免旧图命中。

## 0.1.248
- 修复“返回原版章节”后立刻重新生成 AI 分支选项的问题；返回原版现在只恢复原版章节和原版选项后的 `[剧情生成]` 入口。
- 增加自动选项短暂抑制机制，避免全局 observer 在纯返回原版后马上补回 AI 选项。
- 新增源码级回归测试，防止 `restoreOriginalPassage` 和底部返回按钮再次直接调用 `autoInjectChoices`。

## 0.1.247
- AI 剧情场景生图面板切换剧情后不再自动展示旧缓存图，也不再把上一段生成提示词带入新剧情。
- 场景生图提示词输入框默认保持为空；点击“生成图片”时若为空，会先按当前剧情重新生成提示词再出图。

## 0.1.246
- AI 剧情场景生图默认风格从旧的 DoL 像素素材风切换为视觉小说插画风，避免剧情图被当成小像素图标或素材图生成。
- 场景图缓存默认键同步使用新的场景风格，避免误用旧像素风缓存。

## 0.1.245
- 增加生图最终模型 prompt 的内部调试接口，便于检查玩家设置、风格规则和场景内容是否真正进入最终请求。

## 0.1.244
- 修复场景生图最终 prompt 会重复注入玩家外观/角色列表，导致地点、动作和风格规则被裁掉的问题。
- 场景生图现在优先保留设置里的风格规则、个人追加提示词和构图偏好，再拼接当前剧情的客观画面描述。
- 将硬编码的 visual novel 风格改为中性场景插图描述，避免和玩家选择的绘图风格冲突；同时更新缓存签名，避免复用旧图规则。

## 0.1.243
- 修复玩家外观变量提取时可能优先读到中文 `description/fullDescription`，导致整段外观被当作中文字段清空的问题。
- 生图玩家服装现在优先使用英文物品名/type/id，能稳定带入 hanfu、catgirl bra、paper parasol、feathered hair clip 等变量外观。

## 0.1.242
- 场景生图补强当前剧情提取：中文剧情里的约旦、暗红原石、石柱凹槽、烛光、描摹/嵌入等动作会转成客观英文画面描述。
- 玩家外观不再只依赖侧边栏中文识别；即使侧边栏只识别到油纸伞，也会继续读取游戏穿着变量，补入汉服、内衣、发饰、领饰、手部饰品、发色和发长等信息。
- Temple Jordan 场景地点提取改为神殿内石廊/石柱/烛光环境，不再退成笼统的 temple forest。

## 0.1.241
- 场景生图 prompt 改为“客观画面描述”风格：正向词只写单张画面的主体、地点、动作、光线和镜头，不再把大量禁止规则塞进正向 prompt。
- 将多格/分镜/拼贴/多视角/文字/多人等限制集中到负面词，减少模型把规则当成画面内容来画的问题。
- 场景图 prompt 自动压短到更稳定的长度，并再次更新缓存签名，避免继续复用旧规则图片。

## 0.1.240
- 修复场景生图隐藏提示词仍含“characters positioned / visible interaction”导致模型补多人的问题；无可见对象时改为单主角独自执行当前动作。
- 场景生图新增单画面约束：明确禁止 split-screen、上下分镜、漫画分格、拼贴、多场景并列。
- 生图缓存签名加入场景 prompt 规则版本，隐藏规则更新后不会继续命中旧图缓存。

## 0.1.239
- AIStoryGen 存档状态新增非 API 配置保存：剧情开关、提示词、记忆参数、色色设置等会随游戏存档恢复；API Key、接口地址和模型仍保留在本地浏览器配置中。
- AIPixelGen 绘图配置也随存档保存非 API 项：风格、负面词、构图、角色外观/NPC 外观等会随存档恢复；绘图/LLM 接口、Key、模型保留本地。
- 进一步收紧场景生图 prompt：场景图只从当前 AI 剧情正文判断可见角色，不再从旧 NPC 状态、姿势状态或配置文本里补人物。
- 强化无文字约束：提示词和负面词增加空白纸张、书本、墙面、招牌、屏幕、标签等规则，减少图片里出现文字。

## 0.1.238
- 调整剧情生图 prompt 的角色数量规则：没有明确可见 NPC/实体时允许单主角或环境叙事图，不再默认要求多人同框。
- 场景图负面词移除“solo / one person / only player”等会强行补人的限制，改为禁止额外路人、重复人物、无关人群。
- 只有剧情明确出现可见角色时，才要求主角与该角色同框，并限制不要添加额外旁观者。

## 0.1.237
- 将入口按钮“进入色色模式”改短为“进入色色”。
- 将入口按钮“自定义色色模式”改短为“自定义色色”。
- 对象选择弹窗同步去掉“模式”二字，旧入口文字仍保留识别兼容。

## 0.1.236
- 将入口按钮“色色模式生成”改名为“自定义色色模式”。
- 对象选择弹窗同步改为“选择自定义色色模式的对象”，旧按钮文字仍会被识别用于去重和兼容。

## 0.1.235
- 修复配置恢复顺序：localStorage 中刚保存的设置现在优先于 IndexedDB 备份，避免“色色模块模式”重载后从“两者都开启”被旧备份盖回“AI自由”。
- 保存配置时会先归一化再写入 localStorage 和 IndexedDB，减少旧值残留。

## 0.1.234
- “色色模块模式”下拉框改为选择后立即保存，避免玩家选中“两者都开启”但配置仍停留在旧模式。
- 保存/切换色色模块模式后会立刻刷新原版桥接入口和 AI 自由入口。
- AI 色色模式第一轮点击“回退一轮”时会给出明确提示，不再表现得像按钮无响应。

## 0.1.233
- “色色模块模式”新增“两者都开启”选项。
- AI 剧情底部栏和原版页面补入口现在都会按模式显示：AI自由只显示“色色模式生成”，原版桥接只显示“进入色色模式”，两者都开启时同时显示两个按钮。
- 切换模式后会清理不该出现的旧色色入口，减少页面残留按钮造成的混乱。

## 0.1.232
- 修复 AI 自由色色页不显示生图菜单的问题：`AIStoryGen_Intimate_Mode` 不再被绘图模块误判为工具菜单页。
- 生图剧情提取增加 `.ai-intimate-story`，AI 自由色色正文也能用于生成场景图提示词。

## 0.1.231
- 原版色色/战斗动作桥接没有可执行候选动作时，不再渲染“没有额外动作”的空下拉窗格。
- 只有确实存在可选原版动作时，才显示“选择动作”下拉框。

## 0.1.230
- 修复 AI 自由色色模式中 `[AI_INTIMATE_EVENT]` 结构块未闭合时进入正文的问题。
- AI 自由色色正文改为纯文本渲染，不再交给 SugarCube/Wikifier 执行，避免 `=affection`、`<<...>>` 等模型输出被误当成游戏脚本导致报错。
- 结构块解析支持未闭合和单行字段，避免内部字段污染场景状态栏。

## 0.1.229
- AI 色色模式底部固定栏强制保持单横行显示，AI 菜单按钮、返回原版章节和安全返回会在同一条底部栏内排列。
- 固定栏空间不足时改为横向滚动，不再自动换成上下两排，也不会覆盖原版安全返回。

## 0.1.228
- AI 色色模式固定底栏会把原版自动生成的 `(2) 安全返回` 链接也移动到同一横行内。
- 底栏现在统一承载 AI 操作按钮、返回原版章节链接和安全返回链接，避免安全返回单独掉到下一行或被遮挡。

## 0.1.227
- AI 色色模式固定底栏改为一横行同时显示 AI 操作按钮和原版“返回原版章节/安全返回”链接。
- 原版返回链接仍保留原 wiki 链接与拦截逻辑，只是移动到同一条固定底栏内，避免上下分层或互相遮挡。

## 0.1.226
- 修正 AI 色色模式固定底栏遮住原版“安全返回/返回原版章节”按钮的问题。
- AI 操作栏现在固定在底部安全返回区域上方，并增加页面底部预留空间，确保两个按钮区都能看到并点击。

## 0.1.225
- AI 色色模式底部操作按钮改为真正固定在屏幕底部的按钮栏，不再随页面滚动。
- 固定底栏会按当前游戏内容区域宽度自动定位，窗口大小变化时重新对齐。
- AI 色色专用页会预留底部空间，避免固定按钮遮挡最下面的内容或安全返回区。

## 0.1.224
- AI 色色模式的 `(1) 继续 / 结束并结算 / 回退一轮 / 取消返回` 从 AI 面板内部移到专用章节底部控制区。
- 底部控制区会在页面渲染后再次定位到章节末尾，避免被原版安全返回/可用链接检测区挤到上方。
- 重渲染时会先清理旧底部控制区，防止重复按钮。

## 0.1.223
- 修复 AI 色色模式动态动作解析：当模型把多个部位动作写在同一行时，现在会正确拆到对应部位，不会把“嘴部/左手”等动作误归到“全身”。
- 继续保留 0.1.222 的动态动作开关、固定动作兜底和回退恢复动作列表机制。

## 0.1.222
- AI 色色模式新增“AI 生成色色动作选项”设置，默认开启。
- 独立 AI 色色模式现在会要求模型在每轮剧情后返回下一页各身体部位动作，页面优先显示这些贴合当前姿势的动作。
- 如果模型漏写动作、格式异常或动作数量过少，自动回退到内置固定动作池，避免玩家卡在没有可选行动的页面。
- 回退一轮时会同步恢复上一轮的 AI 动作选项，避免剧情和行动菜单错位。

## 0.1.218
- AI 色色模式每个身体部位都新增“自定义动作”输入，可针对该部位单独写下一步动作。
- 页面底部新增“玩家设定”长输入框，用于填写本场景偏好、风格、边界或互动方向，并随下一轮 AI 生成传入提示词。
- 玩家设定会保存在当前 AI 色色场景状态中，刷新/重渲染时不丢失。

## 0.1.217
- AI 色色模式行动区改用原版 `columnRadioControl` 双列布局。
- 每个部位行动改为列内上下排列，避免所有选项横向挤在一行。
- 自定义动作窗格也跟随双列布局显示。

## 0.1.216
- AI 色色模式行动区改为复用原版 `#listContainer` / `radioControl` 布局骨架。
- 行动选择改为原版式 radio：先选动作，再点 `(1) 继续` 推进 AI 场景。
- 将“双手”拆成“左手/右手”，并把自定义动作作为原版式行动行加入列表。

## 0.1.215
- AI 色色模式行动区改成更接近原版的横向部位排版：左侧部位名，右侧行动项。
- 自定义动作改为同一行动列表里的独立“自定义”窗格，发送时会作为自定义行动传给 AI。
- 精简行动按钮外观，减少卡片感，让页面更像原版行动菜单。

## 0.1.214
- AI 色色模式移除“可选/顺从/反抗行动”切换。
- 可选行动改为按身体部位分组显示，更接近原版按部位选择行动的布局。
- 增加全身、嘴部、双手、胸口、腰臀、腿脚、对方、场景等分组行动，并把所选部位一并传给 AI 叙事。

## 0.1.213
- 进入 `AIStoryGen_Intimate_Mode` 时清理该章节旧版遗留的 “has no usable links” 错误节点。
- 清理只匹配 AI 色色专用章节自己的无可用链接错误，不移除其他原版或模组错误。

## 0.1.212
- “返回原版章节”改为标准 `[[返回原版章节|Start]]` wiki 链接，以满足 DoL 原版可用链接检测。
- 点击该链接时由 AIStoryGen 拦截并执行真实返回逻辑，仍会返回进入 AI 色色模式前的原版章节。

## 0.1.211
- 将 AI 色色专用章节里的隐藏安全链接改成可见的 SugarCube 链接“返回原版章节”，用于满足 DoL 原版“章节必须有可用链接”的检查。
- “返回原版章节”会调用 AIStoryGen 的返回逻辑，回到进入 AI 色色模式前的原版章节，而不是跳到标题页。

## 0.1.210
- 修复独立 AI 色色章节触发 DoL 原版“章节没有可用链接”报错的问题。
- 在 `AIStoryGen_Intimate_Mode` 内加入隐藏的安全返回 wiki 链接，仅用于满足原版章节检查，不显示给玩家。

## 0.1.209
- “色色模式生成”改为进入独立章节 `AIStoryGen_Intimate_Mode`，不再把 AI 色色模式嵌入当前剧情窗口。
- 新增 `<<aiintimatemode>>` 宏和 `ai-intimate.twee`，AI 色色模式现在像原版色色/战斗模式一样切到专用页面显示。
- 取消、结束并结算会返回启动前的原版章节；结算剧情会在返回后写入，避免污染 AI 专用章节。
- 读档/刷新恢复 active 的 AI 色色场景时，会回到 AI 专用章节，而不是把面板贴到普通剧情页。

## 0.1.208
- 修复 AI 色色模式切换“可选/顺从/反抗行动”时可能重复插入整套面板的问题。
- 重渲染前会清理当前章节内所有旧 AI 色色模式面板，包括被隐藏原文包裹或布局管理器移动过的残留节点。

## 0.1.207
- 重做“色色模式生成”的独立 AI 页面：点击后进入类似原版色色/战斗模式的行动页，而不是聊天式“AI 自由亲密场景”面板。
- 新页面包含继续、结束并结算、回退一轮、取消返回、可选/顺从/反抗行动切换和行动按钮；AI 负责推进叙事、姿势、氛围和结算事件。
- 玩家界面只显示中文对象名，内部 NPC key 仅保留给提示词和关系结算使用，避免出现“悉尼(Sydney)”这类混合名称。

## 0.1.206
- 修复“色色模式生成”选择对象后点击“开始生成”偶发无反应的问题。
- 目标选择窗口内的加号、全选、取消、开始生成按钮现在会阻止事件冒泡，避免被全局按钮监听或其他面板逻辑误处理。
- AI 生成入口确认后直接启动 AI 自由亲密场景，原版入口确认后直接进入原版桥接，两个路径不再共用配置分发。

## 0.1.205
- 修复生图完成后点击剧情/色色按钮时页面疯狂闪烁的问题。
- 生图面板排序现在交给 AIStoryGen 的 PanelManager 统一管理，不再安装旧的 AIPixelGen 排序 observer。
- 生图自动注入 observer 会忽略剧情按钮、生图面板、道具面板等 AI 面板自身的移动，避免互相触发重排。

## 0.1.204
- 修复带时间标注的离开链接，例如 `(9) 离开 (0:01)`，仍被错误追加 `[剧情生成]` 的问题。
- 离开类链接现在会先清理编号、AI 标记和 `(H:MM)` 时间，再进入跳过规则。

## 0.1.203
- 修复选项生成 fallback 过宽导致 `[STATS]`、正文段落、标题行被当成剧情按钮的问题。
- 收紧选项生成提示词，禁止在选项接口中输出正文、元数据块或事件块。
- 当前 Edge 现场检测到仍加载 0.1.201 旧页；此版同步后可通过设置页版本号或控制台确认是否加载 0.1.203。

## 0.1.188
- 修复战斗/色色原版动作下拉菜单全部显示“没有可执行的原版动作”的问题。
- 原版动作采集先查当前部位附近；附近为空时，退回当前战斗页可见原版动作作为兜底。
- 仍然不恢复玩家自由输入动作，避免文字动作和原版机制状态分裂。

## 0.1.189
- 修复战斗/色色下拉菜单无法识别原版 radio 动作的问题。
- 动作采集范围从 a/button 扩展到 label，支持 DoL 原版的“label + radio input”动作按钮。
- 执行动作时点击原版 label，让原版动作变量先被选择，再由玩家继续原版回合。

## 0.1.190
- 战斗/色色动作下拉改为显示“额外动作”：从原版 `$actionDefaults` 中读取当前部位可用动作，并排除当前页面已经显示的原版 radio 动作。
- 隐藏动作执行时直接写入对应原版动作变量，例如 `$leftaction`、`$mouthaction`，再由玩家点击“继续”进入原版结算。
- 下拉占位从“选择原版动作”改为“选择动作”，空列表显示“没有额外动作”。
- 修复动作框样式，外框现在会完整包住下拉和执行按钮。

## 0.1.191
- 修复额外动作执行时未稳定写入原版动作变量的问题。
- 动作候选现在保留 `aiVarKey/aiCode`，执行按钮优先直接写入 `$leftaction`、`$rightaction` 等原版动作变量。
- 继续保留“选择动作”文案和“只显示额外动作”的行为。

## 0.1.192
- 战斗/色色额外动作菜单移除“执行”按钮，玩家选中下拉动作后立即执行。
- 修复动作框视觉问题，按钮移除后外框只包住自定义动作菜单和状态提示。
- 非胸部部位不再显示 breast/chest 类动作，避免嘴、阴道、臀部等菜单出现咬胸、吮吸胸部等不合部位的选项。

## 0.1.193
- 战斗/色色动作下拉框加宽，避免自定义动作区域看起来过窄。
- 选择额外原版动作后自动写入短期记忆，用于后续 AI 剧情衔接。
- 同一部位同一动作在短时间内去重，避免误点反复刷入短期记忆。

## 0.1.194
- 进一步加宽战斗/色色动作下拉框区域，使自定义动作框更明显、更容易点击。

## 0.1.195
- 战斗/色色动作短期记忆增加尾部去重，避免连续记录完全相同的原版动作。

## 0.1.196
- 原版固定结算动作不再显示“剧情生成”，例如制作药膏、制造肥皂、清点物资、扫地、缝补衣服、放松等。
- 避免 AI 接管会立即修改时间、物品、关系或状态的原版动作，减少剧情与游戏数据不同步风险。

## 0.1.197
- 修复战斗/色色两列布局下自定义动作下拉框错位的问题。
- 下拉框现在优先按原版动作变量组定位，例如阴茎动作会插到 radiobutton-penisaction 对应的原版动作组下面。

## 0.1.198
- 修复 Jordan 在 AI 选项中被翻成“乔丹”的问题，兜底译名改为原版汉化“约旦”。
- 人名本地化优先读取游戏运行态 NPCName/displayname_lan，手写表只作为兜底，减少 NPC 名称与原版不一致。
- 剧情和选项提示词加入 NPC 标准名表，要求 AI 使用原版中文名。

## 0.1.199
- 设置页新增“启用 AI 色色模式”总开关，默认开启。
- 关闭后不再显示 AI 剧情选项里的“进入色色模式”按钮，也不显示原版色色/战斗界面的 AI 战斗叙事、额外动作和结束战斗辅助控件。
- 保存设置时会即时清理已显示的 AI 色色相关面板，并恢复原版战斗/色色页面文字。

## 0.1.200
- 记忆系统新增“长期重点事件”提取规则，高重要度的地点路线、人物关系、剧情线索/物品、未完成目标会直接进入长期记忆。
- AI_EVENT 的 memoryTags 提示改为剧情向标签，不再推荐把普通资源状态当作长期记忆重点。
- 长期记忆仍然排除原版属性、技能、穿着、金钱数值、普通库存和静态房间描述，避免和游戏原生数据重复。

## 0.1.201
- 新增 AI 自由亲密场景试做版，作为独立于原版色色/战斗系统的剧情模块。
- 设置页新增“色色模块模式”：AI自由、原版桥接、每次询问；默认使用 AI自由。
- AI自由场景会独立记录参与者、姿势、氛围、回合和回退历史，结束时再通过事务系统把关系/状态/记忆结算回游戏。
- 统一存档结构新增 intimateScene 字段，支持未结束的 AI自由场景随存档恢复。

## 0.1.202
- 修复宽松模式下悉尼/Sydney 等已知 NPC 在场但不显示色色入口的问题。
- “进入色色模式”固定为原版桥接入口，“色色模式生成”固定为 AI 自由亲密场景入口，避免两个系统混用。
- 目标选择窗口按入口区分文案：原版入口显示进入原版模式，AI生成入口显示开始生成。

## 0.1.218
- AI 自由亲密场景的每个身体部位分组都新增“自定义动作”输入框，玩家可按部位补充具体动作方向。
- 页面底部新增“玩家设定”长输入格，用于填写本场景的长期设定、偏好、风格、边界或希望 AI 延续的互动方向。
- 自定义动作和玩家设定会进入 AI 自由亲密场景提示词，避免只改变界面而不影响后续叙事。

## 0.1.219
- 修复返回原版页面后按钮/生图面板反复闪烁的问题。
- 剧情核心面板现在插入时会直接避开生图面板，不再先放到最底部再由 observer 反复纠正。
- PanelManager 增加游戏运行态布局冷却合并，减少多个 observer 同时触发导致的 DOM 震荡。
- AIPixel 生图模块在 Story PanelManager 接管排序时，不再因面板移动重新清理和注入生图面板。

## 0.1.220
- AI 自由色色模式扩展身体部位行动池，新增更多肢体、体位、引导、插入、寸止、释放/拔出等行动。
- 新增“性器”“后穴”行动分组，每个分组仍保留独立自定义动作输入框。
- AI 自由色色模式新增欲望、好感、敏感度、主动度、紧张五项进度条。
- AI_INTIMATE_EVENT 新增 `intimateStats` 字段，AI 可按回合调整这些独立场景数值；回退一轮时会同步恢复数值快照。

## 0.1.221
- AI 自由色色模式的“继续 / 结束并结算 / 回退一轮 / 取消返回”移动到页面底部，靠近返回原版章节的位置。
- 底部操作按钮增加分隔线和间距，避免和部位选项、玩家设定混在一起。

## 0.1.252
- 设置页新增“界面布局模式”：自动识别、强制手机布局、强制平板布局、强制桌面布局。
- 自动模式会根据屏幕宽度和宽高比给页面添加手机/平板/桌面布局类；接近方形的平板屏幕会自动进入平板布局。
- AI 自由色色界面的部位行动区改为响应式网格：手机单列、平板两列、桌面自动多列。
- 底部固定操作栏增加安全区预留和最大高度限制，减少平板方屏下遮挡内容、按钮挤压和布局错乱。

## 0.1.253
- 修复 0.1.252 布局规则过度影响桌面/现有色色行动区的问题。
- 桌面默认恢复原来的行动区排列；响应式网格只在手机、平板或自动识别为方屏平板时启用。
- 新增 `ui-0.1.44.css`，避免浏览器缓存继续使用上一版错误样式。

## 0.1.254
- 进一步收窄响应式布局作用域：不再给 `html/body` 添加布局类，只在 AI 自由色色面板和该面板的底部按钮栏上添加。
- 页面加载时会清理旧版本遗留在 `html/body` 上的布局类，避免影响原版天气、tips、设置页和其他原版 UI。
- 新增 `ui-0.1.45.css`，所有平板/手机布局规则都改为面板局部生效。

## 0.1.255
- 撤销 0.1.252-0.1.254 的屏幕布局检测/强制重排机制，不再根据手机、平板或桌面模式给页面或 AI 面板添加布局 class。
- 移除设置页中的“界面布局模式”，避免废弃选项继续影响排查。
- 保留旧布局 class 的清理逻辑，防止刷新后旧页面残留标记继续干扰原版或 AI UI。

## 0.1.256
- 修复 AI 战斗叙事隐藏原版文字时可能误隐藏原版图片/画布的问题。现在会硬保护 canvas、img、picture、svg、video、mainCanvas、lighting 以及包含这些元素的容器。

## 0.1.259
- 自定义色色模式新增“原版动态图预览”桥接层：进入场景时只捕获当前原版 `NPCList` 中能被原版战斗渲染器识别的角色，没有匹配到的自定义对象保持空缺，不强行生成。
- 预览渲染采用只读快照：临时套用 `V.NPCList / V.position / V.combat` 调用原版 `<<animateCombat>>`，渲染后立即恢复原变量，避免污染原版流程和存档。
- AI 色色结构化输出新增 `visualPose`，用于把 AI 姿势映射到原版 `missionary / doggy / wall` 预览姿势。
- 新增 `ui-0.1.49.css`，为自定义色色中的原版动态图预览提供独立滚动与轻量边框。

## 0.1.257
- 修复原版色色/战斗动态姿势图在 AI 战斗叙事页缺失的问题：保护原版 `.combat-grid` 等外层画布容器，避免只保护内部 canvas 时外层仍被隐藏。
- 当原版“图片”和“战斗图片”选项均开启、但当前页缺少原版战斗图时，会在 AI 战斗叙事下方补渲染一次原版 `<<animateCombat>>` 图层。
- 新增 `ui-0.1.47.css`，给补回的原版动态图保留轻量间距，不改动原版内部布局。

## 0.1.258
- 撤销 0.1.257 中主动补渲染原版 `<<animateCombat>>` 的逻辑，避免图片出现在错误时机或错误位置。
- 保留原版动态姿势图容器保护：AI 战斗叙事只负责不隐藏原版已有 `.combat-grid`，不再自行创建原版图片。
- 新增 `ui-0.1.48.css`，移除补渲染容器样式，让原版图片位置继续由原版页面控制。
















## 2026-06-30 v0.1.315 - 公开版 / 亲密扩展拆包

- 将发布结构拆为两个 ModLoader 包：
  - `AIStoryGen.zip` / `织境空间-v0.1.315-公开版.zip`：公开版，保留 AI 剧情、AI 选项、记忆、道具、地点、事务系统和剧情生图。
  - `AIStoryGenIntimateAddon.zip` / `织境空间-亲密扩展-v0.1.315-附属包.zip`：附属包，依赖公开版，用于解锁亲密/战斗相关入口与 `AIStoryGen_Intimate_Mode` passage。
- 公开版 boot 不再打包 `ai-intimate.twee`，只加载公开剧情与生图入口。
- 新增 `intimate-addon.js`，附属包加载后登记 `window.AIStoryGenIntimateAddon`，并启用亲密相关设置页和入口。
- 主程序增加亲密扩展门禁：没有附属包时，即使旧配置里保存过开启状态，也不会显示亲密/战斗入口。
- 同步脚本 `sync-ai-mods.ps1` 支持双包打包、双包注入测试 HTML、按 boot name 写入浏览器 ModLoader 缓存。
- 验证：
  - `node --check`：`aiMacro.js`、`aipixel-module.js`、`intimate-addon.js` 通过。
  - Node 回归：核心、主测试、面板、地点、状态存储、事件校验通过。
  - 浏览器 smoke：`AIStoryGen 0.1.315` 加载成功，错误列表为空，双包缓存可见。
## 0.1.315 / AIPixelGen 内置绘图
- 将“织境空间”拆成公开版 `AIStoryGen` 与附属包 `AIStoryGenIntimateAddon`：公开版只保留 AI 剧情与剧情生图，亲密/色色相关 Twee 与入口放入附属包。
- 新增同步脚本 `-PublicOnly` 模式：只打包、注入、同步公开版，不会再自动把亲密扩展塞进测试页。
- 测试 HTML 生成时会剔除旧的 `AIStoryGenIntimateAddon` 嵌入数据；公开版同步后也会从浏览器 ModLoader 缓存移除旧附属包，避免“只加载公开版但仍显示色色设置/按钮”。
- 已重新生成并覆盖 `D:\Dol\织境空间-v0.1.315-公开版.zip`。验证公开版页面无亲密扩展、成人开关 false、设置页无色色/战斗叙事设置、页面无色色按钮；`aiMacro_node_test.js` 304/304 通过，浏览器 smoke 通过。

## 2026-07-01 v0.1.315 - 发布前公开包净化

- 取消“点击版本号 20 次解锁隐藏内容”的机制；现在只按附属包加载状态判断是否显示亲密相关内容：公开版不显示，加载 `AIStoryGenIntimateAddon` 后自动显示。
- 公开版 `boot.json` 移除 `ai-native-sex-action-bridge.js`，公开 zip 不再包含该桥接文件；附属包新增并加载该桥接模块。
- 主宏改为运行时动态读取附属包桥接模块，避免附属包后加载时桥接失效。
- 普通 AI_EVENT 输出格式改为公开版使用中性的 `presentTargets` 字段；只有附属包加载后才额外要求 `sexTargets`。
- 修复 API 未配置中文提示乱码，清理生图模块重复默认 key，并让 AIPixelGen 模块版本跟随主版本。
- 已重新覆盖：
  - `D:\Dol\织境空间-v0.1.315-公开版.zip`
  - `D:\Dol\织境空间-亲密扩展-v0.1.315-附属包.zip`

## 2026-07-02 生图双人构图加固
- 加强命名角色同屏约束：当剧情明确出现 Alex 等角色时，最终生图提示词会写明“玩家和命名角色必须作为两个独立可见人物同框”，并禁止漏画、裁掉或用空枕头/被子替代。
- 为 Alex 场景补最低限度视觉身份；床铺剧情会明确 Alex 与玩家同床、在被子下靠近、手臂环住玩家腰。
- 清理弱描述导致的 `and Alex` 重复片段，避免第二角色提示不清楚。
- 已在 Edge 当前 Farm Alex Bedroom 页面实测重新生成图片：新图从单人躺床改善为双人同床画面。
- 已重新覆盖：
  - `D:\Dol\织境空间-v0.1.315-公开版.zip`
  - `D:\Dol\织境空间-亲密扩展-v0.1.315-附属包.zip`
- 验证：
  - `node --check`：`aiMacro.js`、`aipixel-module.js`、`intimate-addon.js`、`ai-native-sex-action-bridge.js` 通过。
  - `aiMacro_node_test.js`：304/304 通过。
  - `aiNativeSexActionBridge_node_test.js`：7/7 通过。
  - `browserSmoke_node_test.js`：7/7 通过。
  - `browserPlaythrough_node_test.js`：9/9 通过。

## 2026-07-01
- 修复 AI 道具提取误判：过滤当前穿戴装备与常见衣物饰品，清理 legacy 的 x1/(gained) 后缀，避免 camisole、tabi、gold chain 等英文/重复装备被当成可拾取 AI 道具。

- 加固旧页面/旧存档的 AI 道具显示与收入路径：可拾取提示渲染和点击收入前都会重新过滤，旧候选全部无效时显示为没有可收入的有效AI道具。

- 优化 AI 道具使用提示：选中道具进入剧情请求时新增 item_effect_context，携带库存详情、分类、来源/描述、使用提示和玩家本轮动作，减少 AI 只补一句道具使用但剧情无变化的问题。

- 修复附属包联动：色色模块 ask 模式改为单入口弹出选择，不再与 both 模式一样直接显示两个按钮。
- 修复 AI_EVENT 列表字段污染：presentCharacters=/presentTargets= 等字段名不再进入角色/对象列表。
- 加固 AI 设置保存：保存设置时同步当前存档内的非本地配置，减少读档后宽容度等选项回退。
- 清理旧 AI 道具脏数据：读取库存时过滤 x1(已使用)/(used)/(consumed) 等旧伪物品。

## 2026-07-01 再次测试修复
- 为 AI 选项生成增加 45 秒超时兜底，避免卡在“AI 正在生成选项”。
- 修正 ask 模式下色色入口只显示一个“色色”按钮，点击后再选择原版/自定义入口。
- 清理旧版已使用 AI 道具伪条目，并过滤 AI_EVENT 人物列表字段名污染。
- 设置变更会同步进当前存档态；选项提示词增加金额准确性约束。
- 已通过节点测试与 Edge 实测一轮。

## 2026-07-01 Farm Bed 报错修复
- 修复 Farm Bed / wearoutfit 在 ModI18N 汉化服装名查找缺失时触发的 cn_name_cap 报错。
- 为服装中文名索引增加运行时兜底，不改原版穿衣、衣柜或存档流程。
- 修复启动早期清理色色 UI 时状态仓库尚未初始化导致的控制台警告。
- bunny slippers 缺失项显示为“兔子拖鞋”。
- 已在 Edge 当前 Farm Bed 页面重载验证：页面错误为空，控制台无新增 warning/error。

## 2026-07-02 生图动作层修复
- 修复卧室/床铺场景的生图提示词被默认成“站立或走动”的问题；现在会优先识别躺下、睡觉、坐下、跪下等动作。
- 补充“艾利克斯”译名识别，避免 Alex 场景被误判成只有玩家一人。
- 生图主体层改为明确输出“玩家角色和 Alex”等组合，减少模型漏画当前互动角色。
- 已在 Edge 当前 Farm Alex Bedroom 页面验证：提示词包含“躺在床上、Alex 手臂环住腰、放松睡姿”，页面无错误，控制台无新增 warning/error。
- 已重新覆盖：
  - `D:\Dol\织境空间-v0.1.315-公开版.zip`
  - `D:\Dol\织境空间-亲密扩展-v0.1.315-附属包.zip`

## 2026-07-03 公开版/亲密扩展拆分稳定化
- 清空测试服务器 `AIStoryGen/tmp/modList.json` 中残留的远端旧包 `AIStoryGen-0.1.233.zip`，避免测试页额外加载旧公开包并覆盖新版 passage。
- 公开版恢复稳定主脚本，同时补回公开功能仍需要的 `ai-combat-narrator.js` 与 `ai-visual-capabilities.generated.js`；亲密专用桥接模块继续放在附属包。
- 亲密扩展改用 SugarCube 兼容入口读取 `Engine/Story/State/Wikifier`，修复新版环境下自定义页面跳转失败。
- `AIStoryGen_Intimate_Mode` 增加隐藏返回链接，修复 DoL 的 “has no usable links” 检测报错。
- 验证结果：
  - 公开版单独加载：`AIStoryGen 0.1.315`，亲密扩展未加载，页面可响应。
  - 公开版 + 亲密扩展加载：两个包均为 `0.1.315`，自定义色色页可进入，30 个动作按钮正常，回退一轮可恢复上一轮。
  - 直接大幅删除主脚本亲密兼容代码会导致公开版启动卡死；后续源码净化必须在稳定主脚本上小批量抽离。
- 最终包：
  - `D:\Dol\WovenRealm-packages\release\v0.1.315\织境空间-v0.1.315-公开版.zip`
  - `D:\Dol\WovenRealm-packages\release\v0.1.315\织境空间-v0.1.315-亲密扩展.zip`

## 2026-07-03 公开版配置继续净化
- 公开版移除 `<<aiintimatemode>>` 宏注册；当前亲密扩展已用自己的 passage + JS 渲染，不再依赖公开包中的亲密宏入口。
- 公开版新增 `AIStoryGen.registerConfigFields()` 配置扩展点，基础配置表只保留公开功能。
- 将 `启用 AI 色色模式`、`色色模块模式`、`色色宽容度`、`AI 生成色色动作选项` 四项迁移到亲密扩展加载时注册，公开版单独加载时不再生成这些设置项。
- 验证结果：
  - 公开版单独加载：`AIStoryGen 0.1.315`，亲密扩展未加载，`<<aiintimatemode>>` 不存在，设置页无“色色/亲密”文本，页面无错误。
  - 公开版 + 亲密扩展加载：亲密扩展已加载，四个亲密设置项通过注册接口出现，设置页无错误。
- 已重新覆盖发布包，并额外复制到 `D:\Dol` 根目录方便手动加载：
  - `D:\Dol\WovenRealm-packages\release\v0.1.315\织境空间-v0.1.315-公开版.zip`
  - `D:\Dol\WovenRealm-packages\release\v0.1.315\织境空间-v0.1.315-亲密扩展.zip`

## 2026-07-03 发布前配置检查
- 检查公开版默认 API/模型设置：API Key 为空，普通剧情模型为 `deepseek-v4-flash`，高质量后台为 `deepseek-v4-pro`，高质量接口为 DeepSeek 官方兼容接口。
- 检查绘图默认设置：绘图 LLM 默认改为 `deepseek-v4-flash`；图片 API Key 为空；特殊/本地生图 endpoint、checkpoint、姿势控制模型文件名均为空；默认风格保持 DoL 类型。
- 清理公开包里的个人环境痕迹：移除旧本地局域网 endpoint 判断 `192.168.50.141:45595`，移除通用 `model.safetensors` 兜底文件名。
- 公开版 zip 扫描：未发现 API key / Bearer token / 个人局域网 IP / `.safetensors` 默认文件名；未发现亲密设置标签。
- 干净 Edge profile 实测公开版设置：`AIStoryGen 0.1.315` 正常加载，亲密扩展未加载，设置页不包含亲密内容，页面错误列表为空。
- 测试通过：
  - `aiCore_node_test.js`：20/20
  - `aiApiClient_node_test.js`：7/7
  - `aiConfigUI_node_test.js`：6/6
  - `aiPanelManager_node_test.js`：36/36
- 最终公开包已覆盖到：
  - `D:\Dol\织境空间-v0.1.315-公开版.zip`
  - `D:\Dol\WovenRealm-packages\release\v0.1.315\织境空间-v0.1.315-公开版.zip`

